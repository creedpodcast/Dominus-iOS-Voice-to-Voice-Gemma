# Kokoro model data — too large for git, re-download via Scripts/download_kokoro_models.sh
kokoro/
# sherpa-onnx binaries — re-download from GitHub releases
DominusApp/sherpa-onnx.xcframework/
DominusApp/onnxruntime.xcframework/
DominusApp/sherpa-onnx-xcframework/
# Claude worktrees
.claude/
# Build artifacts
Build Stack Notes/
